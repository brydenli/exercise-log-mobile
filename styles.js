import { StyleSheet } from 'react-native';

const styles_app = StyleSheet.create({
	container: {
		flex: 1,
		backgroundColor: '#fff',
		alignItems: 'center',
		marginTop: "10%"
	},
});

export default styles_app;
